[phases.setup]
nixPkgs = ['nodejs_20', 'npm', 'python3', 'pkg-config']

[phases.install]
cmds = ['npm ci --include=dev --ignore-scripts']

[phases.build]
cmds = ['npm run build']

[phases.postbuild]
cmds = ['npm prune --production --silent', 'npm cache clean --force']

[start]
cmd = 'npm run start'